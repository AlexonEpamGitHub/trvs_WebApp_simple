@{
    ViewBag.Title = "About";
    ViewBag.Message = "This is the about page.";
}
<main aria-labelledby="title">
    <h2 id="title">@Html.Encode(ViewBag.Title)</h2>
    <h3>@Html.Encode(ViewBag.Message)</h3>

    <p>Use this area to provide additional information.</p>
</main>